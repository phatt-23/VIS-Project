= Wireframe

#figure(
  image("../assets/wireframe/create-commit.png", width: 300pt),
  caption: [Writeframe - Add Commit]
)

#figure(
  image("../assets/wireframe/repo-view.png"),
  caption: [Writeframe - Repository View]
)


/*
== Sign In/Sign Up/Sign Out

#figure(
  grid(
    columns: (auto, auto),
    gutter: 3pt,
    grid.cell(
      image("../assets/skica/sing-in.png")
    ),
    grid.cell(
      image("../assets/skica/sing-out.png")
    ),
    grid.cell(
      image("../assets/skica/sing-up.png")
    )
  ),
  caption: [Skica - Sign In/Sign Up/Sign Out]
)

== Dashboard

#figure(
  grid(
    columns: (auto, auto),
    grid.cell(
      image("../assets/skica/dashboard-empty.png")
    ),
    grid.cell(
      image("../assets/skica/dashboard.png")
    )
  ),
  caption: [Skica - Dashboard]
)

== Search 

#figure(
  grid(
    columns: (auto, auto),
    grid.cell(image("../assets/skica/search.png")),
    grid.cell(image("../assets/skica/search-filters.png")),
  ),
  caption: [Skica - Search]
)

== User Profile View

#figure(
  grid(
    columns: (auto, auto),
    grid.cell( image("../assets/skica/user-profile-view.png") ),
    grid.cell( image("../assets/skica/edit-profile-view.png") ),
  ),
  caption: [Skica - User Profile]
)

== Repository View

#figure(
  grid(
    columns: (auto, auto),
    grid.cell(image("../assets/skica/repo-view.png")),
    grid.cell(image("../assets/skica/repo-view-file-preview.png")),
    grid.cell(image("../assets/skica/edit-repo.png")),
    grid.cell(image("../assets/skica/create-repo.png")),
  ),
  caption: [Skica - Repository],
)

== Issue View

#figure(
  grid(
    columns: (auto, auto),
    grid.cell(image("../assets/skica/issue-view.png")),
    grid.cell(image("../assets/skica/open-issue.png")),
  ),
  caption: [Skica - Issue],
)

== Commit View

#figure(
  grid(
    columns: (auto, auto),
    grid.cell(image("../assets/skica/commit-view.png")),
    grid.cell(image("../assets/skica/commit-view-file-preview.png")),
  ),
  caption: [Skica - Commit],
)

== Create Commit

#figure(
  grid(
    columns: (auto, auto),
    grid.cell(image("../assets/skica/create-commit-empty.png")),
    grid.cell(image("../assets/skica/create-commit-with-files.png")),
    grid.cell(image("../assets/skica/create-commit-file-preview.png")),
  ),
  caption: [Skica - Create Commit],
)

*/
