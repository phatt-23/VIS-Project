@startuml

start

:User selects modified files;
:Enters commit message;
:Clicks "Commit";

if (Has write permission?) then (Yes)
    :System creates a new commit object;
    :Links the commit to the previous one;
    :Updates the branch history;
    :Saves the commit to the database;
    :Displays confirmation of a successful commit;
else (No)
    :Show error "Access denied";
endif

stop

@enduml

