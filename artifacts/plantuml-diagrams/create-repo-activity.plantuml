@startuml

start

:Click on "New Repository";
:Display the repository creation form;
:User enters name, description, and visibility;

if (Is the name valid?) then (Yes)
    :System creates the repository;
    :Save to database;
    :Display the repository page;
else (No)
    :Show error "Invalid name";
endif

stop

@enduml

